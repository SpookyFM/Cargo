// In-Game Editing data structures


struct Script {
  int what;
  int type;
  
  // Ok, here's my thinking: Make an array to remember all of the things that were unhandled. This will be exported to a tool (via local network, or a file, ...)
  // In this class, make a number of simple one-line defautl reactions: Looking at: Say or display, Take -> Get the item, ...
  // Not too fancy, the idea is just that people can try it out, will not replace default AGS operations.
  
};
